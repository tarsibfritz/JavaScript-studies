// We need a function that can transform a number (integer) into a string.

function numberToString(num) {
    const string = String(num);
    return string;
}
  
console.log(numberToString(128));
console.log(typeof numberToString(128));