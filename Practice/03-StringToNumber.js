// We need a function that can transform a string into a number. What ways of achieving this do you know?

const stringToNumber = function(str){
    const number = Number(str)
    return number;
}
  
console.log(stringToNumber("-280"))